# Spike

The live loop lives in [`../../poc`](../../poc). Playwright is a **transport to CDP**, not the product API.

Harvest `compactAx`, classify, trusted click — then delete usage from product packages.
