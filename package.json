{
  "name": "tyto",
  "private": true,
  "type": "module",
  "description": "AI-first Chrome/Edge control: accessibility tree in, trusted CDP out, prompt session on disk.",
  "workspaces": [
    "packages/core",
    "packages/protocol",
    "packages/fs",
    "packages/secrets",
    "packages/host",
    "packages/sdk",
    "packages/cdp",
    "packages/llm",
    "packages/mcp",
    "packages/perch"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p packages/core && tsc --noEmit -p packages/protocol && tsc --noEmit -p packages/fs && tsc --noEmit -p packages/secrets && tsc --noEmit -p packages/host && tsc --noEmit -p packages/sdk && tsc --noEmit -p packages/mcp && tsc --noEmit -p packages/llm && tsc --noEmit -p packages/cdp && tsc --noEmit -p packages/perch",
    "secrets:scan": "node scripts/check-secrets.mjs",
    "lint:imports": "node scripts/check-core-imports.mjs",
    "check": "npm run lint:imports && npm run secrets:scan && npm test && npm run typecheck",
    "start": "tsx packages/host/src/cli.ts",
    "poc": "npx tsx poc/run.ts"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.4"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
